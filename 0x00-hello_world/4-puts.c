#include <stdio.h>
/**
 * This prints a set of strings using 'puts' command
 * the main function serves as the entry point
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
